#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int char_to_ascii(char ch)
{
    int flag=ch;
    return flag;
}

int main() 
{

    char ch;
    scanf("%c",&ch);
    int ans = char_to_ascii(ch);
    printf("%d",ans);
    
    return 0;
}